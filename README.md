
  # StartInProduct

  This is a code bundle for StartInProduct. The original project is available at https://www.figma.com/design/ZN99af5JHJtpuN2gqm6gfF/StartInProduct.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  